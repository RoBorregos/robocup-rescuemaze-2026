"""Standalone entrypoint for target detection.

The implementation lives in `target_detector.py` so it can be reused from
`Vision/detector.py` and other scripts.
"""

from target_detector import main


if __name__ == "__main__":
    main()
